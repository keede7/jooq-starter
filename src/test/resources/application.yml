
spring:
#  sql:
#    init:
#      mode: always
#      schema-locations: schema.sql
  datasource:
    hikari:
      jdbc-url: jdbc:h2:~/test;
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true